<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supperdrug.customerconsentform">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainMenuAdminActivity" android:label="@string/main_menu_admin">>

        </activity>
        <activity android:name=".activities.MainMenuActivity" android:label="@string/main_menu">>

        </activity>
        <activity android:name=".activities.CreateCustomerActivity" android:label="@string/create_customer">>

        </activity>
        <activity android:name=".activities.CreateStaffActivity" android:label="@string/create_staff">>

        </activity>
        <activity android:name=".activities.SearchCustomerActivity" android:label="@string/search_customer">>

        </activity>
        <activity android:name=".activities.SearchCustomerResultsActivity" android:label="@string/search_customer">>

        </activity>
        <activity android:name=".activities.TreatmentsResultsActivity" android:label="@string/treatments">>

        </activity>
        <activity android:name=".activities.SignatureAndAgreementActivity" android:label="@string/signature_and_agreement">>
        </activity>


	<!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->

        <activity android:name=".activities.UploadActivity" android:label="@string/upload">>

        </activity>

    </application>

</manifest>